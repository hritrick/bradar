# Frontend

This frontend runs on `Next.js` App Router.

## Scripts

- `yarn dev` starts the local dev server on `http://localhost:3000`
- `yarn build` creates a production build
- `yarn serve` runs the production server
- `yarn lint` runs ESLint

## Environment

- `NEXT_PUBLIC_BACKEND_URL=http://localhost:8000`
